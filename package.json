{
    "name": "sinapcode-web",
    "version": "1.2.0",
    "private": true,
    "scripts": {
        "dev": "cd apps/web && next dev",
        "build": "cd apps/web && npx prisma generate && next build",
        "start": "cd apps/web && next start",
        "lint": "cd apps/web && next lint",
        "test": "cd apps/web && jest",
        "test:watch": "cd apps/web && jest --watch",
        "test:coverage": "cd apps/web && jest --coverage"
    },
    "dependencies": {
        "next": "14.1.0"
    },
    "devDependencies": {}
}