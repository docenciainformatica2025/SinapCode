{
    "name": "sinapcode-monorepo",
    "version": "2.2.0",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev": "npm run dev -w sinapcode-web-app",
        "build": "npm run build -w sinapcode-web-app",
        "start": "npm run start -w sinapcode-web-app",
        "lint": "npm run lint -w sinapcode-web-app",
        "test": "npm run test -w sinapcode-web-app"
    }
}